[project]
name = "task-tracker"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[project.scripts]
task-cli = "taskcli.cli:main"
